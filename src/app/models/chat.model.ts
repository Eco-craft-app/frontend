export interface ChatRes {
  bin: string;
  details: {
    material: string;
    size: string;
    variety: string;
  };
  explanation: string;
  item: string;
}

export interface Chat {
  Photo_URL: string
}
